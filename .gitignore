__pycache__/
*.pyc
*.pyo
*.db
*.sqlite3
.env
uploads/*
!uploads/.gitkeep
.DS_Store
# React/ReactDOM UMD files are committed to the repo (static/) so they load
# reliably without depending on a successful Docker npm install at build time.
package.json
node_modules/
